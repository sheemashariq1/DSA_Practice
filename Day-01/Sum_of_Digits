class Solution
{
public:
    int sumOfDigits(int n)
    {
        int sum = 0;
        int rem;
        while (n > 0)
        {
            rem = n % 10;
            sum += rem;
            n /= 10;
        }
        return sum;
    }
};