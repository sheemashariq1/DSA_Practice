#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    void evenOdd(int a, int b)
    {
        if (a % 2 == 0)
        {
            cout << a << endl;
            cout << b << endl;
        }
        else
        {
            cout << b << endl;
            cout << a << endl;
        }
    }
};
