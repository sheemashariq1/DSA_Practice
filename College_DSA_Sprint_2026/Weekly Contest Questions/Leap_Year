#include <iostream>
#include <vector>
#include <string>
using namespace std;

// Write C++ code here
void solve()
{
    int N;
    cin >> N;
    if ((N % 400 == 0) || (N % 4 == 0 && N % 100 != 0))
    {
        cout << "YES";
    }
    else
    {
        cout << "NO";
    }
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    solve();
    return 0;
}