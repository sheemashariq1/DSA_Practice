#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    int sumOfSeries(int n)
    {
        // code here
        long sum = 0;
        for (int i = 1; i <= n; i++)
        {
            sum += i * i * i;
        }
        return sum;
    }
};